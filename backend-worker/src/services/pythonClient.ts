/**
 * PYTHON CLIENT SERVICE
 * - Interacts with: Your `quant-engine` FastAPI server.
 * - Purpose: A helper function that makes the actual fetch() calls to your Python backend.
 * - Handles timeouts, error handling, and JSON parsing so your route files stay clean.
 */