{
  "name": "brightbet-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "cd ../frontend && npm install && npm run build && cd ../backend-worker"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "wrangler": "^3.100.0",
    "@cloudflare/workers-types": "^4.20240620.0"
  }
}
